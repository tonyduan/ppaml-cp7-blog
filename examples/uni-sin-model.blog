random Real theta ~ UniformReal(-5,5);

fixed Real sigma2 = 1;

random Real X(Timestep t) ~ 
  if t == @0 then Gaussian(0, sigma2)
  else Gaussian(sin(theta * X(prev(t))), sigma2);

random Real Y(Timestep t) ~ Gaussian(X(t), sigma2);

query theta;
